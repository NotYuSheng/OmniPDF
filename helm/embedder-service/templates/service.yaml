apiVersion: v1
kind: Service
metadata:
  name: {{ include "embedder-service.fullname" . }}
  labels:
    {{- include "embedder-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "embedder-service.selectorLabels" . | nindent 4 }}

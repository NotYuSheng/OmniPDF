apiVersion: v1
kind: Service
metadata:
  name: {{ include "docling-translation-service.fullname" . }}
  labels:
    {{- include "docling-translation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docling-translation-service.selectorLabels" . | nindent 4 }}

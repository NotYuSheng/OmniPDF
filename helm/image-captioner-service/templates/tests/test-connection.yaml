apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "image-captioner-service.fullname" . }}-test-connection"
  labels:
    {{- include "image-captioner-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '--timeout=10', '{{ include "image-captioner-service.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never

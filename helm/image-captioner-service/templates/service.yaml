apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-captioner-service.fullname" . }}
  labels:
    {{- include "image-captioner-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-captioner-service.selectorLabels" . | nindent 4 }}

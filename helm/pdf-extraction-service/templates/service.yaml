apiVersion: v1
kind: Service
metadata:
  name: {{ include "pdf-extraction-service.fullname" . }}
  labels:
    {{- include "pdf-extraction-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pdf-extraction-service.selectorLabels" . | nindent 4 }}

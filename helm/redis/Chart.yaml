apiVersion: v2
name: redis
description: OmniPDF Redis - In-memory session store and caching

type: application
version: 0.1.0
appVersion: "7.0.15-alpine"

# Chart icon for Helm repositories and UIs
icon: file://../assets/logo.svg

# Keywords for chart search
keywords:
  - redis
  - cache
  - session-store
  - in-memory
  - omnipdf
apiVersion: v1
kind: Service
metadata:
  name: {{ include "minio.fullname" . }}
  labels:
    {{- include "minio.labels" . | nindent 4 }}
    service.omnipdf.local/type: storage
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: minio-api
      protocol: TCP
      name: minio-api
    - port: {{ .Values.service.consolePort }}
      targetPort: minio-console
      protocol: TCP
      name: minio-console
  selector:
    {{- include "minio.selectorLabels" . | nindent 4 }}

---
{{- if .Values.service.separateConsoleService }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "minio.fullname" . }}-console
  labels:
    {{- include "minio.labels" . | nindent 4 }}
    service.omnipdf.local/type: storage-console
  {{- with .Values.service.consoleAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.consoleType }}
  ports:
    - port: {{ .Values.service.consolePort }}
      targetPort: minio-console
      protocol: TCP
      name: minio-console
  selector:
    {{- include "minio.selectorLabels" . | nindent 4 }}
{{- end }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minio.fullname" . }}-test-connection"
  labels:
    {{- include "minio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '--timeout=10', '{{ include "minio.fullname" . }}:{{ .Values.service.port }}/minio/health/live']
  restartPolicy: Never

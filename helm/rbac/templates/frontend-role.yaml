{{- if .Values.rbac.frontend.enabled }}
---
# Role for Frontend Service - Limited to PDF Processor communication
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: {{ .Values.namespace }}
  name: frontend-role
  labels:
    app.kubernetes.io/name: omnipdf-rbac
    app.kubernetes.io/component: frontend
rules:
{{- if .Values.rbac.frontend.canCallProcessor }}
# Service Discovery - only PDF Processor based on C4 diagram
- apiGroups: [""]
  resources: ["services", "endpoints"]
  resourceNames: 
    - "pdf-processor-service"
  verbs: ["get", "list"]
{{- end }}

# Secret access for service communication
# NOTE: Per-service secret isolation - frontend has its own secret with service credentials
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames:
    - "frontend-secrets"  # Contains service credentials
  verbs: ["get"]

# ConfigMap access for frontend configuration  
- apiGroups: [""]
  resources: ["configmaps"]
  resourceNames:
    - "frontend-config"
  verbs: ["get", "watch"]

---
# RoleBinding for Frontend Service
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: frontend-binding
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: omnipdf-rbac
    app.kubernetes.io/component: frontend
subjects:
- kind: ServiceAccount
  name: {{ .Values.serviceAccounts.frontend }}
  namespace: {{ .Values.namespace }}
roleRef:
  kind: Role
  name: frontend-role
  apiGroup: rbac.authorization.k8s.io
{{- end }}